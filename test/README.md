# Befedit Test Suite

Run `make` to build tests. The resulting test file will be located in `./bin/test_befedit`

Running the executable (expects `befedit` to be installed) will generate several files in an
`./output/` folder. Compare the output to their counterparts in `./expected/`
